services:
  postgres:
    image: postgres:16
    container_name: hypercore_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hypercore
    ports:
      - "5432:5432"
    volumes:
      - hypercore_pgdata:/var/lib/postgresql/data
volumes:
  hypercore_pgdata:
